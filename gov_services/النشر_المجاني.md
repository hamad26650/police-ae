# 🆓 دليل النشر المجاني - خطوات بسيطة

## الخيار 1: Railway.app (الأسهل) ⭐

### الخطوات:

1. **سجل حساب:**
   - اذهب إلى: https://railway.app
   - سجل بحساب GitHub

2. **رفع المشروع:**
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المشروع

3. **إضافة متغيرات البيئة:**
   - اضغط على المشروع → Settings → Variables
   - أضف:
     ```
     DJANGO_SECRET_KEY=your-secret-key
     DJANGO_DEBUG=False
     ALLOWED_HOSTS=your-app.railway.app
     EMAIL_HOST_USER=Project.test85@outlook.com
     EMAIL_HOST_PASSWORD=your-app-password
     ```

4. **إضافة قاعدة بيانات:**
   - اضغط "New" → "Database" → "PostgreSQL"
   - Railway سينشئ `DATABASE_URL` تلقائياً

5. **تشغيل Migrations:**
   - اضغط على المشروع → "Deployments" → "View Logs"
   - أو استخدم Railway CLI:
     ```bash
     railway run python manage.py migrate
     railway run python manage.py createsuperuser
     ```

6. **جمع الملفات الثابتة:**
   - Railway سيقوم بذلك تلقائياً من `Procfile`

✅ **جاهز!** الموقع سيكون على: `your-app.railway.app`

---

## الخيار 2: Render.com

### الخطوات:

1. **سجل حساب:**
   - اذهب إلى: https://render.com
   - سجل بحساب GitHub

2. **إنشاء Web Service:**
   - اضغط "New" → "Web Service"
   - اربط GitHub repo
   - الإعدادات:
     - **Build Command:** `pip install -r requirements.txt && python manage.py collectstatic --noinput`
     - **Start Command:** `gunicorn gov_services.wsgi:application --config gunicorn_config.py`

3. **إضافة قاعدة بيانات:**
   - اضغط "New" → "PostgreSQL"
   - اختر "Free" plan

4. **إضافة متغيرات البيئة:**
   - في Web Service → Environment:
     ```
     DJANGO_SECRET_KEY=your-secret-key
     DJANGO_DEBUG=False
     ALLOWED_HOSTS=your-app.onrender.com
     DATABASE_URL=<من PostgreSQL service>
     EMAIL_HOST_USER=Project.test85@outlook.com
     EMAIL_HOST_PASSWORD=your-app-password
     ```

5. **تشغيل Migrations:**
   - استخدم Render Shell:
     ```bash
     python manage.py migrate
     python manage.py createsuperuser
     ```

✅ **جاهز!** الموقع سيكون على: `your-app.onrender.com`

---

## الخيار 3: PythonAnywhere

### الخطوات:

1. **سجل حساب:**
   - اذهب إلى: https://www.pythonanywhere.com
   - سجل حساب مجاني

2. **رفع الملفات:**
   - استخدم Files tab لرفع الملفات
   - أو استخدم Git:
     ```bash
     git clone your-repo-url
     ```

3. **إعداد Virtual Environment:**
   - في Bash console:
     ```bash
     mkvirtualenv --python=/usr/bin/python3.10 myenv
     pip install -r requirements.txt
     ```

4. **إعداد قاعدة البيانات:**
   - استخدم MySQL المجاني المدمج
   - أو SQLite (أبسط)

5. **إعداد Web App:**
   - اضغط "Web" tab → "Add a new web app"
   - اختر "Manual configuration" → Python 3.10
   - في WSGI file، عدّل:
     ```python
     import sys
     path = '/home/yourusername/gov_services'
     if path not in sys.path:
         sys.path.append(path)
     
     from gov_services.wsgi import application
     ```

6. **إضافة متغيرات البيئة:**
   - في Web tab → Static files
   - أضف متغيرات في WSGI file أو استخدم ملف `.env`

✅ **جاهز!** الموقع سيكون على: `yourusername.pythonanywhere.com`

---

## ⚠️ ملاحظات مهمة:

1. **الحدود المجانية:**
   - Railway: 500 ساعة/شهر
   - Render: قد ينام الموقع بعد 15 دقيقة عدم استخدام
   - PythonAnywhere: محدود لكن مستقر

2. **الملفات الثابتة:**
   - تأكد من تشغيل `collectstatic`
   - استخدم WhiteNoise (موجود في requirements.txt)

3. **قاعدة البيانات:**
   - Railway/Render: PostgreSQL مجاني
   - PythonAnywhere: MySQL أو SQLite

4. **البريد الإلكتروني:**
   - تأكد من إعدادات Outlook في متغيرات البيئة
   - استخدم App Password

---

## 🎯 التوصية:

**استخدم Railway.app** - الأسهل والأسرع!

1. سجل بحساب GitHub
2. اربط المشروع
3. أضف المتغيرات
4. جاهز! 🚀

---

## 📞 إذا واجهت مشكلة:

- Railway: https://docs.railway.app
- Render: https://render.com/docs
- PythonAnywhere: https://help.pythonanywhere.com

