# ✅ ميزة رفع الملفات للأدلة والإثباتات

## 📎 نظرة عامة
تم إضافة ميزة رفع الملفات لبلاغات الاحتيال لتمكين المتقدمين من إرفاق الأدلة والإثباتات المطلوبة.

---

## 📁 الملفات المطلوبة

### 1. **إثبات التحويل** 💵
- **الوصف**: كشف حساب بنكي أو إيصال التحويل
- **التفاصيل**:
  - إذا تم التحويل عن طريق البنك: إرفاق كشف حساب بنكي
  - إذا تم عبر وسائل أخرى: إرفاق إيصال يوضح عملية التحويل
- **الحالة**: يظهر عند اختيار "نعم" للمبلغ المالي
- **الصيغ المدعومة**: PDF, JPG, JPEG, PNG, DOC, DOCX

### 2. **إثبات الاتفاق أو الاحتيال** 📄
- **الوصف**: المحادثات أو المستندات أو أي إثبات آخر
- **التفاصيل**: أي شيء يثبت الاتفاق أو الاحتيال (محادثات واتساب، رسائل، عقود، الخ)
- **الحالة**: يظهر عند اختيار "نعم" للإثباتات
- **الصيغ المدعومة**: PDF, JPG, JPEG, PNG, DOC, DOCX

### 3. **الوكالة القانونية** ⚖️
- **الوصف**: إرفاق الوكالة القانونية (إن وجدت)
- **التفاصيل**: اختياري - فقط إذا كان المتقدم لديه وكالة قانونية
- **الحالة**: يظهر عند اختيار "نعم" للإثباتات
- **الصيغ المدعومة**: PDF, JPG, JPEG, PNG, DOC, DOCX

---

## 🎨 التصميم والواجهة

### المكونات الرئيسية

#### 1. **زر اختيار الملف**
- تصميم أنيق مع أيقونة رفع الملف
- لون متدرج (بنفسجي)
- تأثير hover جذاب

#### 2. **عرض اسم الملف**
- صندوق منقط يعرض "لم يتم اختيار ملف"
- عند اختيار ملف: يتغير اللون إلى الأخضر ويظهر اسم الملف
- نص متحرك إذا كان الاسم طويلاً

#### 3. **رسالة توضيحية**
- أيقونة معلومات
- نص توضيحي لكل حقل

---

## 💾 التخزين والمعالجة

### Backend (views.py)

```python
# استخراج الملفات المرفقة
transfer_proof = request.FILES.get('transfer_proof')
agreement_proof = request.FILES.get('agreement_proof')
legal_power = request.FILES.get('legal_power')

# حفظ الملفات في قاعدة البيانات
for file_obj, file_description in uploaded_files:
    if file_obj:
        # تحديد نوع الملف
        file_extension = file_obj.name.split('.')[-1].lower()
        
        # إنشاء RequestAttachment
        attachment = RequestAttachment.objects.create(
            request=service_request,
            file=file_obj,
            file_type=file_type,
            file_name=f'{file_description} - {file_obj.name}',
            file_size=file_obj.size
        )
```

### Frontend (JavaScript)

```javascript
// معالجة رفع الملفات وإظهار أسماء الملفات
fileInputs.forEach(file => {
    inputElement.addEventListener('change', function() {
        if (this.files && this.files.length > 0) {
            const fileName = this.files[0].name;
            displayElement.textContent = fileName;
            displayElement.classList.add('has-file');
        }
    });
});
```

---

## 🗂️ قاعدة البيانات

### نموذج RequestAttachment

| الحقل | النوع | الوصف |
|-------|-------|-------|
| request | ForeignKey | البلاغ المرتبط |
| file | FileField | الملف المرفق |
| file_type | CharField | نوع الملف (pdf, image, doc, other) |
| file_name | CharField | اسم الملف + الوصف |
| file_size | IntegerField | حجم الملف بالبايت |
| extracted_text | TextField | النص المستخرج (للبحث) |

### مسار التخزين
```
media/attachments/YYYY/MM/DD/filename.ext
```

---

## 🎯 المنطق الشرطي

### متى تظهر حقول رفع الملفات؟

| الشرط | النتيجة |
|-------|---------|
| اختيار "الاحتيال" | تظهر جميع أسئلة الاحتيال |
| اختيار "نعم" للإثباتات | يظهر قسم "الأدلة والإثباتات (مرفقات)" |
| اختيار "نعم" للمبلغ المالي | يصبح "إثبات التحويل" ذو معنى |

---

## 📊 معلومات الملفات في التفاصيل

عند إرسال البلاغ، يتم إضافة معلومات الملفات إلى `request_details`:

```
=== الملفات المرفقة ===
- إثبات التحويل: bank_statement.pdf (245678 bytes)
- إثبات الاتفاق/الاحتيال: whatsapp_chat.jpg (123456 bytes)
- الوكالة القانونية: power_of_attorney.pdf (89012 bytes)
```

---

## 🔒 الأمان والتحقق

### الصيغ المسموحة
```html
accept=".pdf,.jpg,.jpeg,.png,.doc,.docx"
```

### التحقق في Backend
- التحقق من نوع الملف عبر الامتداد
- حفظ حجم الملف للمرجع
- تسجيل كل عملية رفع في logs

---

## 🧪 الاختبار

### خطوات الاختبار:

1. **افتح صفحة تقديم البلاغ**
   ```
   http://127.0.0.1:8000/submit-report/
   ```

2. **املأ البيانات الأساسية**
   - اختر "الاحتيال" من نوع البلاغ

3. **اختر "نعم" للإثباتات**
   - يجب أن يظهر قسم "الأدلة والإثباتات (مرفقات)"

4. **ارفع ملفات**
   - اضغط على "اختر ملف"
   - حدد ملفاً (PDF أو صورة)
   - تحقق من ظهور اسم الملف

5. **أرسل النموذج**
   - تحقق من رسالة النجاح
   - تحقق من logs: `📎 تم حفظ ملف: ...`

---

## 📋 الملفات المعدلة

### 1. `submit_report_form.html`
✅ إضافة `enctype="multipart/form-data"` للنموذج
✅ إضافة 3 حقول رفع ملفات
✅ CSS لتنسيق حقول الملفات
✅ JavaScript لإظهار أسماء الملفات

### 2. `views.py`
✅ استخراج الملفات من `request.FILES`
✅ حفظ الملفات باستخدام `RequestAttachment`
✅ تحديد نوع الملف تلقائياً
✅ إضافة معلومات الملفات لـ `request_details`
✅ تسجيل في logs

### 3. `models.py`
✅ استخدام `RequestAttachment` الموجود (لا حاجة للتعديل)

---

## 💡 ملاحظات هامة

### 1. **حجم الملفات**
- Django لديه حد افتراضي: 2.5 MB
- يمكن تغييره في `settings.py`:
  ```python
  FILE_UPLOAD_MAX_MEMORY_SIZE = 10485760  # 10MB
  DATA_UPLOAD_MAX_MEMORY_SIZE = 10485760  # 10MB
  ```

### 2. **مسار التخزين**
- تأكد من وجود مجلد `media/`
- تأكد من الصلاحيات الصحيحة

### 3. **النص المستخرج**
- يمكن استخدام `extracted_text` للبحث داخل الملفات
- يتطلب مكتبات إضافية (PyPDF2، pytesseract، python-docx)

---

## 🚀 الميزات المستقبلية (اختياري)

### 1. **معاينة الملفات**
- إضافة thumbnail للصور
- معاينة PDF في نافذة منبثقة

### 2. **رفع متعدد**
- السماح برفع عدة ملفات لكل نوع
- استخدام `multiple` attribute

### 3. **التحقق من الحجم**
- إضافة JavaScript للتحقق من حجم الملف قبل الرفع
- عرض رسالة خطأ إذا كان الملف كبيراً جداً

### 4. **شريط التقدم**
- إضافة progress bar لرفع الملفات الكبيرة

---

## ✅ الحالة

**تم التنفيذ بنجاح!** ✨

- ✅ تصميم أنيق وسلس
- ✅ منطق شرطي ذكي
- ✅ حفظ آمن في قاعدة البيانات
- ✅ تسجيل شامل في logs
- ✅ دعم جميع الصيغ الشائعة
- ✅ تجربة مستخدم ممتازة

---

**تاريخ الإضافة:** 17 أكتوبر 2025
**الإصدار:** 2.1


