# 🧠 نظام التصنيف الذكي للبلاغات

تم إضافة نظام ذكي لتصنيف البلاغات تلقائياً إلى **مدني** أو **جزائي** بناءً على تحليل النص والمرفقات.

---

## ✨ المميزات

- ✅ **تصنيف تلقائي ذكي** للبلاغات
- ✅ **تحليل النص** من تفاصيل البلاغ ونص العريضة
- ✅ **استخراج النص** من PDF والصور والمستندات
- ✅ **نسبة ثقة** بالتصنيف
- ✅ **خصوصية 100%** - كل شيء محلي
- ✅ **سريع جداً** - أقل من ثانية

---

## 📦 التثبيت

### 1. تثبيت المكتبات الأساسية

```bash
pip install -r requirements.txt
```

### 2. تثبيت Tesseract OCR (لاستخراج النص من الصور)

#### Windows:
1. حمّل من: https://github.com/UB-Mannheim/tesseract/wiki
2. ثبّت البرنامج
3. أضف المسار للـ PATH: `C:\Program Files\Tesseract-OCR`
4. حمّل ملف اللغة العربية:
   - انسخ `ara.traineddata` إلى `C:\Program Files\Tesseract-OCR\tessdata\`

#### Linux/Mac:
```bash
# Ubuntu/Debian
sudo apt-get install tesseract-ocr tesseract-ocr-ara

# Mac
brew install tesseract tesseract-lang
```

### 3. إنشاء مجلد الملفات المرفقة

```bash
cd gov_services
mkdir -p media/attachments
```

### 4. تحديث قاعدة البيانات

```bash
python manage.py makemigrations
python manage.py migrate
```

---

## 🎯 كيف يعمل؟

### 1. المستخدم يقدم بلاغ:
- يكتب تفاصيل البلاغ
- يكتب نص العريضة (اختياري)
- يرفع ملفات (PDF, صور, Word)

### 2. النظام يحلل:
```
1. يستخرج النص من كل الملفات
2. يدمج النصوص (تفاصيل + عريضة + مرفقات)
3. يبحث عن كلمات مفتاحية جزائية/مدنية
4. يحلل الأنماط والسياق
5. يحدد التصنيف مع نسبة الثقة
```

### 3. النتيجة:
- **مدني 📋** (نزاع مالي، عقد، إيجار، إلخ)
- **جزائي 🚔** (سرقة، اعتداء، احتيال، إلخ)
- **غير محدد ❓** (إذا لم يكن واضحاً)

---

## 📊 أمثلة

### مثال 1: جزائي
```
النص: "تعرضت للاعتداء من قبل شخص وقام بضربي وكسر باب منزلي"
الملف: صورة للباب المكسور

→ التصنيف: جزائي 🚔 (ثقة: 92%)
```

### مثال 2: مدني
```
النص: "لم يدفع المستأجر مبلغ الإيجار منذ 3 أشهر"
الملف: عقد الإيجار PDF

→ التصنيف: مدني 📋 (ثقة: 88%)
```

---

## ⚙️ الإعدادات

### في `settings.py`:

```python
# مجلد حفظ الملفات
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

### أنواع الملفات المدعومة:

- **PDF**: `.pdf`
- **صور**: `.jpg`, `.jpeg`, `.png`, `.bmp`, `.gif`
- **Word**: `.doc`, `.docx`

### الحد الأقصى:

- **عدد الملفات**: 10 ملفات
- **حجم الملف**: 5 MB لكل ملف

---

## 🔧 التخصيص

### إضافة كلمات مفتاحية جديدة:

في `services/utils/ai_classifier.py`:

```python
# إضافة كلمة جزائية
self.criminal_keywords = {
    'كلمة جديدة': 10,  # الوزن
    ...
}

# إضافة كلمة مدنية
self.civil_keywords = {
    'كلمة جديدة': 10,
    ...
}
```

---

## 🚀 الترقية المستقبلية (AraBERT)

للحصول على دقة أعلى (~95%+)، يمكن ترقية النظام إلى AraBERT:

```bash
# فك التعليق في requirements.txt
pip install transformers torch arabert
```

ثم تعديل `ai_classifier.py` لاستخدام النموذج.

---

## 📈 الإحصائيات

في Dashboard الموظفين، يمكنك رؤية:
- ✅ نوع التصنيف (مدني/جزائي)
- ✅ نسبة الثقة
- ✅ أسباب التصنيف
- ✅ الملفات المرفقة
- ✅ النص المستخرج

---

## ❓ استكشاف الأخطاء

### OCR لا يعمل:
```bash
# تأكد من تثبيت Tesseract
tesseract --version
```

### PDF لا يُقرأ:
```bash
# تأكد من تثبيت PyPDF2
pip install PyPDF2
```

### Word لا يُقرأ:
```bash
# تأكد من تثبيت python-docx
pip install python-docx
```

---

## 📞 الدعم

للمزيد من المساعدة، راجع الكود في:
- `services/utils/ai_classifier.py` - المصنف الذكي
- `services/utils/text_extractor.py` - مستخرج النص
- `services/views.py` - معالجة البلاغات

---

✅ **النظام جاهز للاستخدام!** 🚀






